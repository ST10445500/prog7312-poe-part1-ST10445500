server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Blazor works out the route in the browser, so a deep link has to come back
    # with index.html rather than a 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
